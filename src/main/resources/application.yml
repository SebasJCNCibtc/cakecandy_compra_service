server:
  port: 8085

spring:
  application:
    name: compra-service
  datasource:
    url: jdbc:mysql://localhost:3306/compra_service
    username: root
    password: mysql
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate.format_sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true
  security:
    oauth2:
      resource-server:
        jwt:
          secret-key: MiSuperClaveSegura1234567890!@#$

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true